#include "main.h"

/**
 * _abs - will compute absolute value of passed integer.
 * @n: passed integer.
 *
 * Return: The absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
