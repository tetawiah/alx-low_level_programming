#include "main.h"

/**
 * _abs -will computes the absolute value of integer passed.
 * @n: passed integer to be computed.
 *
 * Return: The absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
